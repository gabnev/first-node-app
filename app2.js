// ********************
//        Modules
// ********************
// every node application has at least one file (module) that we call MAIN MODULE

console.log(module); // not a global object!

// in node every file is a module
// variables and functions are scoped to that module - not the global

